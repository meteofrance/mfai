name: Deploy Sphinx Docs

on:
  push:
    branches:        
      - sphinx

permissions:
    contents: write

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Set up Python 3.10
              uses: actions/setup-python@v6
              with:
                python-version: '3.10'
                cache: 'pip'
            
            - name: Install dependencies
              run: |
                pip install -e ".[docs]"

            - name: Generate API docs
              run: |
                python3 -m sphinx api-doc -o doc/api mfai

            - name: Build docs
              run: |
                make -C doc html

            - name: Deploy to Github Pages
              run: |
                python3 -m ghp-import -n -p -f doc/_build/html
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}